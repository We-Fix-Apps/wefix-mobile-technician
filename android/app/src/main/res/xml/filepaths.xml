<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- For cache directory -->
    <cache-path
        name="cache"
        path="." />

    <!-- For external files directory -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- For internal files directory -->
    <files-path
        name="files"
        path="." />
</paths>

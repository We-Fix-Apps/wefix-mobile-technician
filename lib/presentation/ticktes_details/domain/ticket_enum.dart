enum TicketStatus { init, loading, success, failure }

enum StartTicketStatus { init, loading, success, failure }

enum CompleteTicketStatus { init, loading, success, failure }

enum ToolStatus { init, loading, success, failure }

enum AddToolsStatus { init, loading, success, failure }

enum MaterilsStatus { init, loading, success, failure }

enum AddMaterilsStatus { init, loading, success, failure }

enum DeleteMaterilsStatus { init, loading, success, failure }

enum MaintenancesStatus { init, loading, success, failure }

enum UpdateMaintenancesStatus { init, loading, success, failure }

enum TicketDetailsType { emergency, corrective, preventive }

enum TicketDetailsStatus { pending, inprogress, completed, canceled }

enum MaterialStatus { pending, accepted, rejected }

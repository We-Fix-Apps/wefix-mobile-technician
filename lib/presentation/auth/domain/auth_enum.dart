enum LoginState { init, loading, success, failure }

enum SendState { init, loading, success, failure }

enum RegisterState { init, loading, success, failure }

enum UploadFileState { init, loading, success, failure }

enum UploadFileType { image, voice, cv }
